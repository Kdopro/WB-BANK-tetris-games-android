<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFrameworks>net10.0-android</TargetFrameworks>

		<OutputType>Exe</OutputType>
		<AssemblyName>TetrisAndroid</AssemblyName>
		<RootNamespace>Tetris</RootNamespace>
		<UseMaui>true</UseMaui>
		<SingleProject>true</SingleProject>
		<ImplicitUsings>enable</ImplicitUsings>
		<Nullable>enable</Nullable>

		<MauiXamlInflator>SourceGen</MauiXamlInflator>

		<!-- Display name -->
		<ApplicationTitle>Тетрис</ApplicationTitle>

		<!-- App Identifier -->
		<ApplicationId>ru.wbbank.tetris</ApplicationId>

		<!-- Versions -->
		<ApplicationDisplayVersion>5.1</ApplicationDisplayVersion>
		<ApplicationVersion>51</ApplicationVersion>

		<!-- Poco 8 Pro: флагман 2026, Android 16 (API 36), 16 KB memory page size.
		     targetSdk = 36 (совпадает с ОС устройства; Android НЕ блокирует установку по targetSdk).
		     min 24 (Android 7.0). Нативные библиотеки выравниваются под 16 KB (обязательно для
		     устройств со страницей памяти 16 КБ, иначе приложение падает при старте). -->
		<SupportedOSPlatformVersion>24.0</SupportedOSPlatformVersion>
		<AndroidNativeLibrary16KBAligned>true</AndroidNativeLibrary16KBAligned>
	</PropertyGroup>

	<ItemGroup>
		<!-- App Icon -->
		<MauiIcon Include="Resources\AppIcon\appicon.svg" ForegroundFile="Resources\AppIcon\appiconfg.svg" Color="#12121C" />

		<!-- Splash Screen -->
		<MauiSplashScreen Include="Resources\Splash\splash.svg" Color="#12121C" BaseSize="128,128" />

		<!-- Images -->
		<MauiImage Include="Resources\Images\*" />

		<!-- Custom Fonts -->
		<MauiFont Include="Resources\Fonts\*" />

		<!-- Raw Assets (also remove the "Resources\Raw" prefix) -->
		<MauiAsset Include="Resources\Raw\**" LogicalName="%(RecursiveDir)%(Filename)%(Extension)" />
	</ItemGroup>

	<ItemGroup>
		<PackageReference Include="Microsoft.Maui.Controls" Version="$(MauiVersion)" />
		<PackageReference Include="Microsoft.Extensions.Logging.Debug" Version="10.0.0" />
	</ItemGroup>

</Project>
